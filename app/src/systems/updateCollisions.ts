import { System } from ".";
import { STATE } from "../state";

export class UpdateCollisions implements System {
    public update() {
        for (const {} of STATE.query({ with: [] })) {
        }
    }
}
